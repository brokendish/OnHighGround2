# NEW-RTE-002A fixture (primary): osrm-walking and martin SERVICE
# DEFINITIONS entirely absent (not merely stopped). Real unmodified
# nginx.conf still statically references both names, so nginx MUST fail to
# start (CONTAINER_EXIT) at config-load time -- this is the case under
# test. `backend` mock still present so the failure is attributable
# specifically to the missing osrm-walking/martin definitions, not to
# `backend` also being absent.
services:
  frontend:
    image: nginx:alpine
    volumes:
      - ../../../../nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./empty_html:/usr/share/nginx/html:ro
  backend:
    image: nginx:alpine
